{% if 'openconfig-relay-agent:state' in json_output %}
    {% set counters = json_output['openconfig-relay-agent:state'] %}
    {% for key in counters %}
        {% if 'counters' in  key %}
         {% set counter = counters[key] %}
{{ "BOOTREPLY messages forwarded by the relay agent :" }} {{ counter['bootreply-sent'] }}
{{ "BOOTREQUEST messages forwarded by the relay agent :" }} {{ counter['bootrequest-sent'] }}
{{ "BOOTREQUEST messages received by the relay agent :" }} {{ counter['bootrequest-received'] }}
{{ "DHCP ACK messages sent by the relay agent :" }} {{ counter['dhcp-ack-sent'] }}
{{ "DHCP DECLINE messages received by the relay agent :" }} {{ counter['dhcp-decline-received'] }}
{{ "DHCP DISCOVER messages received by the relay agent :" }} {{ counter['dhcp-discover-received'] }}
{{ "DHCP INFORM messages received by the relay agent :" }} {{ counter['dhcp-inform-received'] }}
{{ "DHCP NACK messages sent by the relay agent :" }} {{ counter['dhcp-nack-sent'] }}
{{ "DHCP OFFER messages sent by the relay agent :" }} {{ counter['dhcp-offer-sent'] }}
{{ "DHCP REQUEST messages received by the relay agent :" }} {{ counter['dhcp-request-received'] }}
{{ "Number of DHCP packets dropped due to an invalid opcode :" }} {{ counter['invalid-opcode'] }}
{{ "Number of DHCP packets dropped due to an invalid option :" }} {{ counter['invalid-options'] }}
{{ "Total number of DHCP packets dropped by the relay agent :" }} {{ counter['total-dropped'] }}
{% endif %}
    {% endfor %}
{% endif %}
