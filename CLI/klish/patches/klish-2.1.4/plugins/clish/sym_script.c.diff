39a40,42
> 	char *cmd = clish_shell__get_full_line(clish_context);
> 	setenv("USER_COMMAND", cmd, 1);
> 
82a86,91
> 	lub_string_cat(&command, " 2> /dev/null");
> 
>         sigset_t sigs;
>         sigemptyset(&sigs);
>         sigaddset(&sigs, SIGINT);
>         sigprocmask(SIG_UNBLOCK, &sigs, NULL);
